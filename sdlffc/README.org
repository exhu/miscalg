LLM USAGE DISCLOSURE: this branch is written with the LLM assistance, sometimes with big patches of generated code.
It's experimental code to play with llms and study ffmpeg+sdl apis, may contain unreviewed parts.


* sdl3 + ffmpeg video playback prototype
Used code from SDL3-3.4.10/test/testffmpeg.c

* Usage and Keys
** SPACE = pause/resume
** LEFT/RIGHT = 5 second seek back/forward
** [ and ] frame back/forward jumps.

** TODO key B = seek to the video start frame
** TODO key E = seek to the video end frame
** TODO key I = set in-marker (start of the cut) to the current video frame time
** TODO key O = set out-marker (last inclusive frame of the cut) to the current video frame time
** TODO key shortcut SHIFT+I = seek to in-marker position
** TODO key shortcut SHIFT+O = seek to out-marker position
** TODO after video is loaded in-marker is set to the first visible frame, out-marker is set to the last visible frame of the video
** TODO timestamp overlay must display IN and OUT time stamps one line below the current timestamp and total duration in yellow colour over 80% opacity black background
** TODO L = loop current video between in and out marks, i.e. video starts playing if it was paused from IN frame and after displaying OUT frame seeks to IN frame and plays again. Pause key pauses/resumes the loop, other keys cancel looping. Repetative L keypress cancells the loop.
** TODO ENTER = print ffmpeg command to stdout that would loselessly ("-ac copy -vc copy) save current video cut to start from IN frame and having OUT as last displayed frame, the new filename is current filename without extension + "_" + inout time in HH_MM_SS format (underscore separates numbers) + original extension.
** TODO add --help command line argument that displays usage help including keyboard shortcuts

* DONE show frame time
* DONE granular seek
* TODO consider gui https://github.com/Immediate-Mode-UI/Nuklear/blob/master/demo/sdl_renderer/nuklear_sdl_renderer.h
* TODO enable hw decoding

* DONE load video, dump stream info
* DONE move decoding to a thread, remove timer?
split handle_next_frame into main thread parts and worker thread parts
* DONE playback video at correct rate
* DONE drop frames when slow
* DONE rewind, fast forward
* DONE play audio

* Build
PKG_CONFIG_PATH=~/.local/lib/pkgconfig/ meson setup _build
ninja -C _build

* sdl3 3.4.10
Install from https://github.com/libsdl-org/SDL/releases/tag/release-3.4.10
e.g. by setting install path to ~/.local
* meson 1.11.1
pipx install meson
* ffmpeg libs ubuntu package version 6.1.1

* debian packages
** libsdl3-dev ? (not avail in ubuntu lts yet?)
** libimgui-dev ?
** libstb-dev ?
sudo apt install libavcodec-dev libavformat-dev libavutil-dev libswscale-dev
