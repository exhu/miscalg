* TODO make video decoding work with multiple videos

* DONE how to link sdl, ffmpeg libs in dub
* DONE design c core that interfaces SDL and ffmpeg libs
** sdl interaction
init, main loop, done, wake main loop -- implemented in C, main loop calls global D functions to
notify of key events, or window resize etc via custom event types (i.e. we don't
need to generate bindings for sdl types, use custom types, or generate bindings
to constants like keycodes )

** DONE study video decoding in sdlffc
av decoder decodes to ringbuffer in a separate thread
texture management in C, but need to split av part from sdl part

** video presenting


** audio decoding

** DONE use dstep to translate keycodes?
** DONE when to rerender scene?



* DONE tried to generate bindings for SDL, but too much work to patch, many C-specific defines and intrinsics, keep to a C core that interfaces C libs and D as the driving logic

* used headers
SDL3/SDL_atomic.h
SDL3/SDL_audio.h
SDL3/SDL_events.h
SDL3/SDL_init.h
SDL3/SDL_keyboard.h
SDL3/SDL_keycode.h
SDL3/SDL_log.h
SDL3/SDL_mutex.h
SDL3/SDL_pixels.h
SDL3/SDL_render.h
SDL3/SDL_surface.h
SDL3/SDL_thread.h
SDL3/SDL_timer.h
SDL3/SDL_video.h
